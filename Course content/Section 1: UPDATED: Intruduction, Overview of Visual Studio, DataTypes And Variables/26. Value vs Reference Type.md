##### value type and reference type

Data types can be categorized into two categories based on how they occupy memory location.

#### Value type in detaul

- Store actual data directly
- Typically stored in the stack
- Include int, float, long, double, char, bool, decimal, struct, enum
- Nullable versions available (int?, double?, etc.)
- Can be stored in heap if part of a reference type

#### Pictorial representation of Value type

![variable diagram](/Users/Jeff/Pictures/ram-diagram.png)

#### Reference type in detail

- Reference type is a variable type which instead of storing the value in memory directly, sotres, the memory location of the actual data.

- The variable here stores the memory reference of the data and not the data directly.

- Reference type data typess are string, class, Array etc.

- When we copy this reference type of a data type it will just copy the memory address of the data so we will then have two variable pointing to the same data.

#### Pictorial representation of Reference type

![variable diagram](/Users/Jeff/Pictures/ram-diagram.png)

