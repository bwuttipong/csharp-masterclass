So this is going to be Zoo table and i'm gonna use a singuar value here
or a singular word, not Zoos plural.
```sql
CREATE TABLE [dbo].Zoo
(
    [Id] INT NOT NULL PRIMARY KEY IDENTITY,
    [Location] NVARCHAR(50) NOT NULL
);

INSERT INTO dbo.Zoo (Id, [Location]) VALUES 
    (1, 'New York'),
    (2, 'Tokyo'),
    (3, 'Berlin'),
    (4, 'Kairo'),
    (5, 'Milan');

CREATE TABLE [dbo].Animal
(
    [Id] INT NOT NULL PRIMARY KEY IDENTITY,
    [Name] NVARCHAR(50) NULL
);

INSERT INTO [dbo].Animal (Id, [Name]) VALUES
    (1, 'Shark'),
    (2, 'Clownfish'),
    (3, 'Monkey'),
    (4, 'Wolf'),
    (5, 'Gecko'),
    (6, 'Crocodile'),
    (7, 'Owl'),
    (8, 'Parrot');

CREATE TABLE [dbo].ZooAnimal
(
    [Id] INT NOT NULL PRIMARY KEY IDENTITY,
    [ZooId] INT NOT NULL,
    [AnimatId] INT NOT NULL,
    CONSTRAINT [ZooFK] FOREIGN KEY (ZooId) REFERENCES Zoo(Id),
    CONSTRAINT [AnimalFK] FOREIGN KEY (AnimalId) REFERENCES Animal(Id)
);

INSERT INTO [dbo].ZooAnimal (Id, ZooId, AnimalId) VALUES
    (1, 1, 1),
    (2, 1, 2),
    (3, 2, 3),
    (4, 2, 4),
    (5, 3, 5),
    (6, 3, 6),
    (7, 4, 7),
    (8, 4, 8),
    (9, 5, 1),
    (10, 5, 2),
    (11, 5, 3),
    (12, 5, 4),
    (13, 5, 5),
    (14, 5, 6),
    (15, 5, 8);
```

[**Table Naming Dilemma: Singular vs. Plural Names [closed]**](https://stackoverflow.com/questions/338156/table-naming-dilemma-singular-vs-plural-names)

let's add some data to that table.
So right click on the table > Show Table Data

And in order to chagne it, I go to open talbe definition.
Server Explorer
> Data Connections
    > connection -> Tables  > Open Table Definition


we can click new query 

this also includes 295. Relationship or Associative Tables mix 2 lectures