in this video, we are going to add the rest of our interface
and we're ganna add one functionality for now, which is going to delete Zoos

```csharp
private void DeleteZoo_Click(object sender, RoutedEventArg e)
{

    string query = "delete from Zoo where id = @ZooId";
    SqlCommand sqlCommand = new SqlCommand(query, sqlConnection);
    sqlCommand.Open();
    sqlCommand.Parameters.AddWithValue("@ZooId", listZoos.SelectedValue);
    sqlCommand.ExecuteScalar();
}
```